<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="at.shockbytes.dante">

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".core.DanteApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".core.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:theme="@style/Dante.ScanTheme"
            android:name=".util.barcode.BarcodeCaptureActivity"
            android:label="@string/label_capture"
            android:screenOrientation="portrait" />
        <activity android:name=".core.DetailActivity" />
        <activity
            android:name=".core.SettingsActivity"
            android:label="@string/label_settings" />
        <activity android:name=".core.BackupActivity"
            android:label="@string/label_backup">

        </activity>
    </application>

</manifest>